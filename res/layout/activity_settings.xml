<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="#e5e5e5"
    
    tools:context=".Settings" >

    <ImageView
        android:id="@+id/nameBlock"
        android:contentDescription="@string/nameBlock"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@android:drawable/bottom_bar"
        android:background="#f2f2f2"/>
    

    <EditText
        android:id="@+id/fishNameSet"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/fishNameTitle"
        android:layout_marginLeft="16dp"
        android:layout_marginRight="2dp"
        android:layout_toRightOf="@+id/fishNameTitle"
        android:textColor="#ffffff"
        android:ems="10"
        android:hint="@string/fishNameSet" >

        <requestFocus />
    </EditText>

    <TextView
        android:id="@+id/fishNameTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/nameBlock"
        android:layout_alignLeft="@+id/nameBlock"
        android:layout_marginBottom="16dp"
        android:text="@string/fishNameTitle"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:textColor="#ffffff" />

    <Button
        android:id="@+id/done"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="30dp"
        android:layout_toRightOf="@+id/fishNameTitle"
        android:onClick="setValues"
        android:text="@string/done" />

</RelativeLayout>
